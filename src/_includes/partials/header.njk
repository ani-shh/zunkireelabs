<header
  class="fixed top-0 left-0 right-0 z-50 transition-all duration-300"
  x-data="{
    scrolled: false,
    activeDropdown: null,
    activeProduct: 'zunkiree-search',
    mobileMenuOpen: false
  }"
  @scroll.window="scrolled = window.scrollY > 50"
  :class="scrolled ? 'bg-white/95 backdrop-blur-xl border-b border-warm-border/50 shadow-sm' : 'bg-transparent'"
>
  <div class="max-w-[1200px] mx-auto px-5 md:px-10">
    <nav class="relative flex items-center justify-between h-[72px]">
      <!-- Logo -->
      <a href="/" class="flex items-center group">
        <img src="/assets/images/zunkireelabs-logo.svg" alt="Zunkiree Labs" class="h-[1.3rem] w-auto" />
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden lg:flex items-center gap-1">
        {% for item in navigation.main %}
          {% if item.hasDropdown %}
            <!-- Dropdown Nav Item -->
            <div
              class="relative"
              @mouseenter="activeDropdown = '{{ item.label }}'; {% if item.dropdownType == 'product' %}activeProduct = '{{ item.items[0].id }}'{% endif %}"
              @mouseleave="activeDropdown = null"
            >
              <button
                class="flex items-center gap-1.5 px-4 py-2 rounded-full font-medium transition-all duration-200"
                :class="activeDropdown === '{{ item.label }}' ? 'bg-warm-surface text-warm-black' : 'text-warm-charcoal hover:text-warm-black'"
              >
                {{ item.label }}
                <svg class="w-4 h-4 transition-transform duration-200" :class="activeDropdown === '{{ item.label }}' ? 'rotate-180' : ''" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </button>
            </div>
          {% else %}
            <!-- Regular Nav Link -->
            <a
              href="{{ item.url }}"
              class="px-4 py-2 rounded-full text-warm-charcoal hover:text-warm-black hover:bg-warm-surface font-medium transition-all duration-200"
            >
              {{ item.label }}
            </a>
          {% endif %}
        {% endfor %}
      </div>

      <!-- CTA Button -->
      <div class="hidden lg:block">
        <a href="/contact/" class="inline-flex items-center px-5 py-2.5 bg-warm-rich-black text-white font-medium text-sm rounded-full hover:bg-warm-black transition-all duration-200">
          Schedule a Demo
        </a>
      </div>

      <!-- Mobile Menu Button -->
      <button
        class="lg:hidden p-2 rounded-lg transition-colors text-warm-charcoal hover:text-warm-black hover:bg-warm-surface"
        @click="mobileMenuOpen = !mobileMenuOpen"
        aria-label="Toggle menu"
        :aria-expanded="mobileMenuOpen"
      >
        <svg x-show="!mobileMenuOpen" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
        <svg x-show="mobileMenuOpen" x-cloak class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>

      <!-- Mega Menu Dropdowns (Centered under nav bar) -->
      {% for item in navigation.main %}
        {% if item.hasDropdown %}
          <div
            class="hidden lg:block absolute top-full left-1/2 -translate-x-1/2 pt-3 w-[700px]"
            x-show="activeDropdown === '{{ item.label }}'"
            x-transition:enter="transition ease-out duration-200"
            x-transition:enter-start="opacity-0 -translate-y-2"
            x-transition:enter-end="opacity-100 translate-y-0"
            x-transition:leave="transition ease-in duration-150"
            x-transition:leave-start="opacity-100 translate-y-0"
            x-transition:leave-end="opacity-0 -translate-y-2"
            @mouseenter="activeDropdown = '{{ item.label }}'"
            @mouseleave="activeDropdown = null"
            x-cloak
          >
            {% if item.dropdownType == 'product' %}
              <!-- Product Mega Menu: Two-Column Layout -->
              <div class="bg-warm-charcoal/95 backdrop-blur-xl rounded-2xl border border-white/10 shadow-2xl overflow-hidden">
                <div class="flex">
                  <!-- Left Column: Products List -->
                  <div class="w-[45%] p-6 border-r border-white/10">
                    <p class="text-xs font-medium text-white/50 uppercase tracking-wider mb-4">Products</p>
                    <div class="space-y-1">
                      {% for product in item.items %}
                        <a
                          href="{{ product.url }}"
                          class="group flex items-start gap-3 p-3 rounded-xl transition-all duration-200"
                          :class="activeProduct === '{{ product.id }}' ? 'bg-white/10' : 'hover:bg-white/5'"
                          @mouseenter="activeProduct = '{{ product.id }}'"
                        >
                          <div class="flex-1">
                            <div class="flex items-center gap-2">
                              <span class="text-white font-medium">{{ product.label }}</span>
                              {% if product.badge %}
                                <span class="text-[10px] font-medium px-2 py-0.5 rounded-full {% if product.badge == 'Primary' %}bg-zunkiree-600 text-white{% else %}bg-white/20 text-white/70{% endif %}">
                                  {{ product.badge }}
                                </span>
                              {% endif %}
                            </div>
                            <p class="text-sm text-white/60 mt-0.5">{{ product.description }}</p>
                          </div>
                          <svg class="w-4 h-4 text-white/40 opacity-0 group-hover:opacity-100 transition-opacity mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                          </svg>
                        </a>
                      {% endfor %}
                    </div>
                  </div>

                  <!-- Right Column: Product Preview Card -->
                  <div class="w-[55%] p-6">
                    {% for product in item.items %}
                      <div
                        x-show="activeProduct === '{{ product.id }}'"
                        x-transition:enter="transition ease-out duration-200"
                        x-transition:enter-start="opacity-0"
                        x-transition:enter-end="opacity-100"
                        class="h-full flex flex-col"
                      >
                        <!-- Icon Card -->
                        <div class="bg-white rounded-2xl p-8 flex items-center justify-center mb-4">
                          {% if product.icon == 'search' %}
                            <svg class="w-16 h-16 text-zunkiree-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                            </svg>
                          {% elif product.icon == 'factory' %}
                            <svg class="w-16 h-16 text-zunkiree-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                            </svg>
                          {% elif product.icon == 'health' %}
                            <svg class="w-16 h-16 text-zunkiree-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                            </svg>
                          {% else %}
                            <svg class="w-16 h-16 text-zunkiree-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
                            </svg>
                          {% endif %}
                        </div>

                        <!-- Product Info -->
                        <h4 class="text-white font-semibold text-lg mb-2">{{ product.label }}</h4>
                        <p class="text-white/70 text-sm leading-relaxed">{{ product.extendedDescription }}</p>

                        <!-- Learn More Link -->
                        <a href="{{ product.url }}" class="mt-auto pt-4 inline-flex items-center gap-1 text-zunkiree-400 text-sm font-medium hover:text-zunkiree-300 transition-colors">
                          Learn more
                          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                          </svg>
                        </a>
                      </div>
                    {% endfor %}
                  </div>
                </div>
              </div>
            {% else %}
              <!-- Simple Mega Menu: Same Width, Single Column -->
              <div class="bg-warm-charcoal/95 backdrop-blur-xl rounded-2xl border border-white/10 shadow-2xl p-6">
                <p class="text-xs font-medium text-white/50 uppercase tracking-wider mb-4">{{ item.label }}</p>
                <div class="grid grid-cols-2 gap-2">
                  {% for subitem in item.items %}
                    <a
                      href="{{ subitem.url }}"
                      class="group flex items-start gap-3 p-4 rounded-xl hover:bg-white/10 transition-all duration-200"
                    >
                      <div class="flex-1">
                        <span class="text-white font-medium group-hover:text-white block">{{ subitem.label }}</span>
                        {% if subitem.description %}
                          <p class="text-sm text-white/60 mt-1">{{ subitem.description }}</p>
                        {% endif %}
                      </div>
                      <svg class="w-4 h-4 text-white/40 opacity-0 group-hover:opacity-100 transition-opacity mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                      </svg>
                    </a>
                  {% endfor %}
                </div>
              </div>
            {% endif %}
          </div>
        {% endif %}
      {% endfor %}
    </nav>
  </div>

  <!-- Mobile Menu -->
  <div
    class="lg:hidden fixed inset-x-0 top-[72px] bg-warm-charcoal/95 backdrop-blur-xl border-b border-white/10 shadow-lg transition-all duration-300 ease-out max-h-[calc(100vh-72px)] overflow-y-auto"
    x-show="mobileMenuOpen"
    x-transition:enter="transition ease-out duration-200"
    x-transition:enter-start="opacity-0 -translate-y-4"
    x-transition:enter-end="opacity-100 translate-y-0"
    x-transition:leave="transition ease-in duration-150"
    x-transition:leave-start="opacity-100 translate-y-0"
    x-transition:leave-end="opacity-0 -translate-y-4"
    @click.away="mobileMenuOpen = false"
    x-cloak
  >
    <div class="max-w-[1200px] mx-auto px-5 py-6">
      <div class="flex flex-col gap-2">
        {% for item in navigation.main %}
          {% if item.hasDropdown %}
            <!-- Mobile Dropdown Section -->
            <div x-data="{ open: false }">
              <button
                @click="open = !open"
                class="w-full flex items-center justify-between px-4 py-3 text-white hover:bg-white/10 rounded-xl font-medium transition-all"
              >
                {{ item.label }}
                <svg class="w-5 h-5 text-white/60 transition-transform duration-200" :class="open ? 'rotate-180' : ''" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </button>
              <div x-show="open" x-collapse class="pl-4 mt-1 space-y-1">
                {% for subitem in item.items %}
                  <a
                    href="{{ subitem.url }}"
                    class="flex items-center gap-3 px-4 py-3 text-white/80 hover:text-white hover:bg-white/10 rounded-xl transition-all"
                    @click="mobileMenuOpen = false"
                  >
                    <span>{{ subitem.label }}</span>
                    {% if subitem.badge %}
                      <span class="text-[10px] font-medium px-2 py-0.5 rounded-full {% if subitem.badge == 'Primary' %}bg-zunkiree-600 text-white{% else %}bg-white/20 text-white/60{% endif %}">
                        {{ subitem.badge }}
                      </span>
                    {% endif %}
                  </a>
                {% endfor %}
              </div>
            </div>
          {% else %}
            <!-- Mobile Regular Link -->
            <a
              href="{{ item.url }}"
              class="px-4 py-3 text-white hover:bg-white/10 rounded-xl font-medium transition-all"
              @click="mobileMenuOpen = false"
            >
              {{ item.label }}
            </a>
          {% endif %}
        {% endfor %}

        <!-- Mobile Contact Link -->
        <a
          href="/contact/"
          class="px-4 py-3 text-white hover:bg-white/10 rounded-xl font-medium transition-all"
          @click="mobileMenuOpen = false"
        >
          Contact
        </a>

        <!-- Mobile CTA -->
        <div class="mt-4 pt-4 border-t border-white/10">
          <a
            href="/contact/"
            class="flex items-center justify-center w-full px-5 py-3 bg-zunkiree-600 text-white font-medium rounded-xl hover:bg-zunkiree-700 transition-all"
            @click="mobileMenuOpen = false"
          >
            Schedule a Demo
          </a>
        </div>

        <!-- Mobile Social Links -->
        <div class="flex items-center justify-center gap-3 mt-4 pt-4 border-t border-white/10">
          <a href="{{ site.social.github }}" class="w-10 h-10 rounded-lg bg-white/10 hover:bg-white/20 flex items-center justify-center text-white/70 transition-colors" aria-label="GitHub">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"/></svg>
          </a>
          <a href="{{ site.social.twitter }}" class="w-10 h-10 rounded-lg bg-white/10 hover:bg-white/20 flex items-center justify-center text-white/70 transition-colors" aria-label="Twitter">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
          </a>
          <a href="{{ site.social.linkedin }}" class="w-10 h-10 rounded-lg bg-white/10 hover:bg-white/20 flex items-center justify-center text-white/70 transition-colors" aria-label="LinkedIn">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</header>
